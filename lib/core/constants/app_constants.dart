import 'package:flutter/material.dart';

/// App color constants
class AppColors {
  static const Color primary = Color(0xFF22A45D);
  static const Color primaryDark = Color(0xFF1A8049);
  static const Color secondary = Color(0xFF010F07);
  static const Color background = Colors.white;
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);

  // Text colors
  static const Color textPrimary = Color(0xFF010F07);
  static const Color textSecondary = Color(0xFF868686);
  static const Color textHint = Color(0xFFB8B8B8);

  // Rating color
  static const Color rating = Color(0xFF22A45D);

  // Navigation colors
  static const Color navSelected = Color(0xFF22A45D);
  static const Color navUnselected = Color(0xFF868686);
}

/// App dimension constants
class AppDimensions {
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
  static const double paddingExtraLarge = 32.0;

  static const double radiusSmall = 8.0;
  static const double radiusMedium = 12.0;
  static const double radiusLarge = 16.0;

  static const double iconSize = 24.0;
  static const double iconSizeSmall = 20.0;
  static const double iconSizeLarge = 32.0;
}
